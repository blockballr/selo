# No shipped component folds Poseidon today. Kept for when the close path
# is wired into one; wasm-ld's 1 MiB default is not enough for it.
[target.wasm32-wasip2]
rustflags = ["-C", "link-arg=-zstack-size=8388608"]
